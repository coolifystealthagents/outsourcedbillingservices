import { Header, Footer } from '../components';import { blogPosts, site } from '../data';export default function Blog(){return <><Header/><main className='section'><div className='container'><p className='eyebrow'>{site.brand}</p><h1>Billing staffing guides</h1><p className='lead'>Plan the queue, choose the first tasks, vet a provider, and onboard a Filipino billing specialist.</p><div className='cards'>{blogPosts.map(p=><a className='card' href={`/blog/${p.slug}`} key={p.slug}><h3>{p.title}</h3><p>{p.excerpt}</p><span className='pill'>{p.minutes} min read</span></a>)}</div></div></main><Footer/></>}
